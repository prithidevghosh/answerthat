Your previous plan was rejected by the invariant kernel, which is deterministic code, not a
model — its verdict is final and its reasons are exact.

Rejected plan:
{plan}

Reasons:
{reasons}

Emit a corrected plan that does not repeat these problems. If no valid plan exists for this
command, return an empty operations list.
